# Default ProGuard rules. NextClass ships with minification disabled, but keep
# these here so a release build with R8 enabled still works out of the box.
-dontwarn java.lang.invoke.StringConcatFactory
